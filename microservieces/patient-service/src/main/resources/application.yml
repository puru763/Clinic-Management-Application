spring:
  application:
    name: patient-service

  datasource:
    url: jdbc:mysql://localhost:3306/clinick-application
    username: root
    password: Krishnani@7634
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
